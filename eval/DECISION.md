# 로컬 추출 모델 선정 — 결정 v2.5 (2026-07-17)

**결정: 7B-FT(합성 전 클래스 학습, q4_K_M) ollama 서빙이 신선 프로토콜에서 parity 전판 PASS,
62 → 13.9s/장(4.5배). 프로덕션 채택 후보 확정 — 채택 여부·전환 계획은 사용자 결정.**

## v2.5 — LoRA-FT 본학습 (2026-07-17): 7B가 32B의 정확도 게이트를 서빙 경로에서 통과했다

### 최종 구성 (채택 후보)

- 모델: `qwen2.5vl:7b-ft2-q4` — Qwen2.5-VL-7B-Instruct + LoRA(합성 6클래스 2,400장 300step
  + FX증강 1,300장 100step 계속학습) 머지 → GGUF q4_K_M + mmproj f16.
  가중치 `/home/omr/workspaces/ft-spike/out/7b-ft2-*.gguf`, Modelfile `eval/ft/Modelfile.7bft2-q4`.
- 입력: ×0.5 LANCZOS 28px 스냅(v2.3과 동일), 프롬프트 prompt4e, NP=2, num_ctx 8192.
- **실측(신선 프로토콜: 인스턴스 재시작 + metrics 사후검증)**: wall 111s/8장 = **13.9s/장**.
  분해: prefill 37s(541 t/s), decode 145s(17.6 t/s). 32B 62s/장 대비 **4.5배**.
- **정확도**: 재현율 31/31, 환각 0, value·qty·price·cost·broker·accountType·qty_src 전부
  31/31, 게이트 침묵, 컨트롤 4종 경보 정상. soft 미스는 32B PASS 런과 동일 2종(name '미국 달러'
  표기, price_src computed)뿐.

### 본학습 경과 (스파이크 → 최종)

1. 렌더러를 실화면 8장의 전 클래스 6종으로 확장(`eval/ft/render_synth.py`), 정답은 클래스별
   32B e0_fresh PASS 패턴을 표본으로. 계좌목록 화면은 계좌번호·예금주(가짜)를 화면에 렌더하되
   정답 name에서 배제 — 스모크의 이름 표류를 정면 겨냥. 사용자 육안 대조 시트 6장 전달.
2. 2,400장 300step(1 epoch, 6.8h) → **HF bf16 held-out parity 전판 PASS** (스모크의 미학습
   클래스 실패 전부 소멸).
3. **서빙 경로 함정 발견(이 판의 핵심 교훈)**: 같은 가중치가 ollama(llama.cpp)에선 q4/q8/**f16
   전부** 160229(USD) 한 화면에서 실패 — 정밀도별로 다른 오답(qty에 1·환율 1509.9 등).
   양자화가 아니라 **추론 구현의 수치 미세차가 얇은 결정 마진을 뒤집은 것**. HF PASS는 서빙
   PASS를 보증하지 않는다 — 판정은 반드시 배포 경로(ollama)에서.
4. 처방: FX 800장(+타 클래스 500, 밝기·대비·JPEG 품질 증강)으로 100step 계속학습(2.2h) →
   마진 확보 → **q4에서도 PASS**. 오답 모드였던 행이 정확히 복구(qty=6923.28, value=10453460).

### 운영 메모

- 학습/변환 환경: `ft-spike/venv`(torch 2.8 jetson-ai-lab), llama.cpp(변환·quantize),
  LLaMA-Factory v0.9.3. gguf 설치가 numpy 2.x를 끌어올려 Jetson torch와 충돌 → **1.26.4 고정**.
- 프로덕션 전환 시 필요한 것(사용자 결정 대기): ① 앱 파이프라인에 ×0.5 리샘플 전처리 추가
  ② 11434에 7b-ft2-q4 등록(모델 저장소는 ollama 유저 홈 — sudo 필요) ③ prompt4e로 교체
  ④ 앱 UI 개편 시 렌더러 갱신+재학습(반나절)이 유지보수 경로.
- 6s/장(10장 1분) 목표 대비: 13.9s/장은 미달이나, 3B-FT(동일 레시피)면 ~6~8s/장 사정권 —
  단 3B의 기본 출력 붕괴(E5) 전력상 성공 확률은 7B보다 낮음. 필요 시 별도 스파이크.

---

# (v2.4) 로컬 추출 모델 선정 — 결정 v2.4 (2026-07-17)

**결정: LoRA-FT 스파이크 GO — 합성 데이터 2.5h 학습만으로 7B의 실증 실패 2종(병합셀 열 매핑,
USD 바인딩)이 held-out 실화면에서 완치됨. 전 화면 클래스로 확장 학습이 다음 단계.**

## v2.4 — LoRA-FT 스파이크 (2026-07-17): 바늘이 크게 움직였다

### 셋업 (Orin 온디바이스, 전 과정 로컬 — 민감정보 반출 0)

- 합성 렌더러 `eval/ft/render_synth.py`: 실패 화면 2종(mPOP 종합잔고 병합셀표, SMART 외화예수금)
  레이아웃을 PIL로 재현(실화면은 기하·팔레트 참조로만, 픽셀·값 전부 가짜). 산술 일관 무작위 값,
  2줄 줄바꿈·소액 5원·자동투자 중복행·잔액0 증거금 함정 포함. 정답 = prompt4e 규칙 그대로의
  11칸 JSON. train 440장 + val 24장, 실화면과 동일 처리(×0.5 LANCZOS 28스냅).
- 학습: LLaMA-Factory **v0.9.3**(py3.10 지원 마지막 버전) + Qwen2.5-VL-7B-Instruct bf16 LoRA
  rank16(LLM만, ViT 동결, 40.4M 파라미터). **110 step ≈ 2h29m**(81s/step, 8샘플/step).
  loss 0.236→0.008(step 20)→0.0001 — 합성 분포는 사실상 암기.
- 평가: held-out 실화면 8장(학습 미사용) ×0.5, HF 추론, 동일 parity 게이트.
  대조군 = 같은 경로의 베이스 7B(어댑터만 제거 — 추론경로·해상도 통제).

### 결과 (base 7B vs FT 7B, 같은 조건 A/B)

| 필드 | base 7B | FT 7B (110 step) |
|---|---|---|
| 재현율 | 28/31 | 30/31 |
| 환각 | 0 | 1 (이름 혼선발) |
| value | 19/28 | **28/30** |
| qty | 17/28 | **30/30** |
| cost | 13/28 | 27/30 |
| broker | 16/28 (증권사명 환각) | 29/30 |
| 게이트 경고 | 6건 | **0건 (침묵)** |

- **학습한 실패 2종 모두 실화면에서 완치**: 160338 병합셀 4행 열 매핑 전부 정확(한화오션
  158,400/277,800/-119,400 ✓), 160229 USD 행 완벽(qty=6923.28, value=10,453,460).
  32B조차 프롬프트 보완이 필요했던 두 지점을 7B가 학습으로 넘었다.
- **잔여 실패는 전부 미학습 화면 클래스에 집중**: 160227(원화예수금)의 name이 '현금'으로,
  160333(계좌목록)의 name이 계좌번호+예금주 이름으로 표류(합성 패턴으로의 쏠림 = 과적합 흔적,
  규칙 3 위반). 즉 실패 원인이 "능력 부족"에서 "학습 커버리지 부족"으로 바뀌었다 — 고칠 방법이
  명확한 종류의 실패.
- 도메인 갭(합성 PIL 렌더 vs 실제 안드로이드 렌더+JPEG)은 학습 클래스에서 문제되지 않았다.

### GO 판정과 다음 단계 (예상 1.5~2.5일)

1. 렌더러를 전 화면 클래스(6종: 상품요약·보유상세 SMART 2종·원화예수금·계좌목록·mPOP 상세)로
   확장 + name 규칙(항목명만, 계좌번호·예금주 금지)을 전 클래스 정답에 명시적으로 반영 — ~1일
2. 2~3k 샘플, 200~300 step 학습(반나절) → held-out parity 31/31 재판정
3. 통과 시 LoRA 머지 → GGUF 변환 → ollama 서빙 → 신선 프로토콜 속도 실측
   (기대: ×0.5 리샘플 + NP=2로 **~13~18s/장**, 32B 대비 3배)
4. 유지보수 리스크 기록: 앱 UI 개편 시 렌더러 갱신+재학습 필요(합성 생성기가 있으므로 재학습
   비용은 반나절 수준). 신규 증권사 추가도 동일 경로.

### 환경 확정 사실 (재현용)

- Jetson NV torch 휠(2.5.0a0)은 **distributed 비활성 빌드** — transformers Trainer 임포트 불가.
  jetson-ai-lab 커뮤니티 인덱스(**pypi.jetson-ai-lab.io**로 이전됨)의 torch 2.8.0+tv 0.23.0
  (cp310/cu126, dist 활성)을 venv 내부에만 설치로 해결. devpi 파일 URL은 `/jp6/cu126/+f/...`.
- LLaMA-Factory 최신은 py≥3.11 요구 → v0.9.3(transformers 4.52.4·peft 0.15.2·trl 0.9.6) 사용.
- 학습 중 GPU 경합: 실험 ollama 인스턴스(11435) 종료 필수. 39GB 사용 피크.
- 스파이크 작업 디렉토리 `/home/omr/workspaces/ft-spike/`(venv·가중치·데이터·어댑터),
  레포에는 재현 스크립트만(`eval/ft/`).

---

# (v2.3) 로컬 추출 모델 선정 — 결정 v2.3 (2026-07-16)

**결정: 리샘플 재개봉 성공 — ×0.5 LANCZOS(28px 스냅) + prompt4e가 parity 전판 통과, 62→44.4s/장(-28%).
프로덕션 적용 여부는 아래 (a)~(d) 결정과 함께 사용자 판단 대기(프롬프트·입력을 동시에 바꾸는 변경).**

## v2.3 — 속도 3차: 리샘플 사다리 실측 + TensorRT 기각 + 역방향(엣지 FT) 선택지 추가

v2.2의 "픽셀 축소 기각"은 알고리즘 미검토 단일 시도였다 → 8알고리즘×3스케일 비교 시트
(사용자 육안 판독: ×0.5 LANCZOS/BOX 뚜렷) 후 재개봉. 전 런 신선 캐시 + 8장 전체 +
`parity.py --no-llm --controls`로만 판정.

### R1 — 리샘플 사다리 실측표

전제(e0_fresh 분해): wall 62s/장 = decode 40.4s(65%) + prefill+ViT ~15s(NP=2 배분) —
리샘플은 prefill+ViT만 단축, 이론 상한 ~46s/장. 단 prefill 축소가 NP=2 decode 간섭도
줄여(유효 3.65→4.96 t/s) 상한을 뚫는다. 28px 배수로 사전 스냅해 ollama 내부 2차 리샘플
(1080→1092 업스케일!)을 회피. 픽스처 생성기: `eval/speed3/gen_resampled.py`
(`snap28(v)=round(v/28)*28`, PIL LANCZOS/BOX±unsharp).

| 구성 | 이미지 토큰 | wall(8장) | s/장 | parity | 판정 |
|---|---|---|---|---|---|
| 원본 (e0_fresh) | ×1.00 (27.7k) | 497s | 62 | 31/31 PASS | 기준 |
| ×0.5 LANCZOS | ×0.27 (7.5k) | 305s | 38 | 30/31+환각1 (160229 바인딩 실패) | FAIL |
| ×0.66 LANCZOS+unsharp | ×0.50 (13.8k) | 377s | 47.1 | 31/31+환각0 (name soft 1) | **PASS** |
| ×0.5 LANCZOS + prompt4d(규칙8 +147자) | ×0.27 | 356s | 44.5 | 30/31 (160333 name=null 부수효과) | FAIL |
| ×0.5 LANCZOS + **prompt4e**(규칙8 +66자) | ×0.27 | 355s | **44.4** | 31/31+환각0 (name soft 1) | **PASS** ← 채택 후보 |
| ×0.4 LANCZOS + prompt4e | ×0.17 (4.7k) | 346s | 43.3 | 30/31 (5원→0 오독) | FAIL — 가독성 한계 |

### '미국 달러' 실패의 기전과 프롬프트 보완 (사용자 요청 심층)

- **기전 = 바인딩 실패(가독성 아님)**: ×0.5는 육안 문제 없음. raw 대조 결과 모델은 원화
  환산액('10,453,460원' 소형 회색 텍스트)을 읽고도 행에 묶지 못해 USD 금액(6923.28)을 value에
  넣고, 잔액 0 '외화증거금' 행을 추가 출력 → 환각+누락 판정. E2와 동일 시그니처.
- **prompt4d(+147자)**: 160229는 완치했으나 무관 화면(160333)의 name이 전부 null로 붕괴 → FAIL.
- **prompt4e(+66자, 규칙 8 최소판)**: 160229 완치 유지 + 160333 정상 + 게이트 침묵 + 컨트롤 4종
  정상 → PASS. `eval/harness/prompt4e.txt`(= prompt4c + 규칙 (8) "외화 예수금: qty=외화 금액,
  value=원화 환산액('…원' 표기 금액). 잔액 0인 예수금·증거금 행은 출력하지 마라.").
- **교훈: 프롬프트 보완은 가능하나 추가 토큰 양에 극도로 민감하다** — 같은 내용의 +147자는
  깨지고 +66자는 통과. 규칙은 최소 표현으로만 추가할 것.
- ×0.4의 실패는 다른 종류(순수 가독성): '5원' 1자리 소액을 0으로 오독. 여기가 픽셀 바닥.

### 퇴거 프로토콜 결함 발견 (측정 신뢰성 — v2.2 교훈의 연장)

같은 이미지를 연속 재실측하자 **텍스트 filler NP발 동시 발사로도 캐시가 안 지워졌다**
(prefill 합계 5s = 신선 110~129s의 1/20; 이전 런들은 매번 SHOTS가 달라 결함이 가려짐).
→ **같은 이미지 재실측은 ollama 인스턴스 재시작이 유일하게 결정적**이며, 매 런
`metrics`의 prefill 처리율(신선 ≈ 155~216 t/s)로 신선 여부를 사후 검증한다.
p4e 1차 런(wall 256s)은 이 결함으로 무효 처리, 재시작 후 재실측이 위 표의 355s다.

### R2 — Orin TensorRT 특화: 기각

| 확인 항목 | 근거 |
|---|---|
| TensorRT-Edge-LLM | Qwen2.5-VL은 3B/7B만 지원(32B 없음). Orin(SM87) FP16/INT8/INT4만 — FP8/NVFP4 금지 |
| TensorRT-LLM jetson 브랜치 | v0.12 고정(JetPack 6.1) — Qwen2.5-VL 멀티모달 미포함 |
| 7B TRT? | 7B는 parity 탈락(v2.1) — 빨라져도 무의미 |
| 물리 상한 | decode 40.4s/장은 대역폭 바운드(21GB@137GB/s) — 엔진 불문. prefill+ViT 전부 무료화해도 62→~46s(-26%) |
| 리스크 | AWQ 재양자화 = q4_K_M과 다른 가중치 → parity 전면 재검증(칼날 위) |

### R3 — 폰 스크린샷 특성 활용 (기각 이력 제외 후)

- **28px 배수 스냅 리샘플**(R1에 채택): 프로세서 격자에 정확히 맞춰 이중 보간 제거 —
  스크린샷의 픽셀 퍼펙트 특성 덕에 단일 고품질 리샘플로 엣지 보존.
- decode 간섭 완화 여지(OLLAMA_NUM_BATCH 등 스케줄링 파라미터): 이번 범위 밖, 기각 아님.
- OCR-primary 논거 보강: 사진 노이즈 0·고정 레이아웃·픽셀 퍼펙트 = OCR 최적 조건.

### R4 — 역방향: 엣지급 모델 기준 정확도 최대화 (조사 — 신규 선택지 (d))

1. **LoRA-FT(합성 데이터)**: 고정 레이아웃 → 가짜 값 렌더로 무한 학습데이터(민감정보 0,
   실화면 8장은 held-out 평가 전용). LLaMA-Factory가 Qwen2.5-VL 지원 + AGX Orin 64GB 검증
   튜토리얼 존재(Seeed). bf16 LoRA면 bitsandbytes(sm_87 휠 없음) 불필요. 7B-FT + 리샘플
   ×0.5 = **~13~18s/장 후보**. 공수 2~5일. 이 태스크는 일반화가 아니라 암기에 가까워
   소형+FT가 정석인 영역.
2. **문서특화 소형모델**: PaddleOCR-VL-0.9B(한국어 edit-dist 0.052, ollama 커뮤니티 포트
   존재 → 0.5일 스파이크 가능), dots.ocr 1.7B 대안. 단 "문서" 가정 vs 앱 UI — 어려운
   화면부터 검증.
3. **추론시간 보강**: N-샘플 셀 투표(불일치=불확실 셀 → 재추출; 합계 게이트와 달리 행
   유실도 잡음), ollama format(JSON 스키마) 제약 — 소형 모델 전용 지렛대.

**후속 실측 — prompt4e는 7B로 이전되지 않는다 (사용자 질문: "소형 모델 실패도 같은 건인가")**:
160229의 USD 바인딩 실패 시그니처 자체는 **모델 불문 공통**(7B 전 프롬프트 변형·3B·qwen3-vl
8B/30B 아카이브 전수 확인 — 전부 value=6923.28 또는 USD행 유실 변형). 그러나 7B+prompt4e
신선 실측(원본 해상도, 26.4s/장): 160229는 여전히 반쪽 행(name·qty=null), 160338 병합셀
열 오배치(매수→price, 손익→value, 평가→cost) 그대로, 160139에선 프롬프트 예시의
'○○증권'을 broker로 복사 + 11칸을 10칸으로 출력 — **규칙 추종 실패가 다발적·독립적**이라
프롬프트 보완으로 못 메운다. E4/E5 기각 유지. 이 격차를 학습으로 메우는 것이 (d)의 요지.

### 운영 사고 2건 (재발 방지)

- **256k ctx 폭주**: num_ctx 미지정 요청 → ollama가 vram 기반 자동 산정 **-c 262144** 리로드
  → 32B 256k KV가 메모리 고갈 → llama-server 커널 D-state(unkillable) → 재부팅.
  **11435로 보내는 모든 요청은 num_ctx:8192 명시.**
- 실험 인스턴스(11435) 기동 시 `OLLAMA_MODELS=/usr/share/ollama/.ollama/models` 필수
  (모델은 systemd 유저 `ollama`의 홈에 있음 — 미지정 시 빈 모델 목록 404).

### 결정 선택지 갱신 (v2.2의 (a)/(b)/(c) 승계)

- (a) OCR-primary 스파이크 — PaddleOCR-VL로 구체화(0.5일 검증 선행 가능)
- (b) 목표 완화 — R1 통과로 "62→**44.4s/장** + 비동기 UX"로 완화 폭 축소
- (c) vLLM/TensorRT — 기각 유지(decode 대역폭 바운드, 32B 경로 부재)
- **(d) 신규: 소형 모델 LoRA-FT**(합성 데이터, 2~5일) — 6s/장 목표에 유일하게
  "정확도를 만들어서" 접근하는 경로. (a) 스파이크 결과가 나쁠 때의 2안.

---

# (v2.2) 로컬 추출 모델 선정 — 결정 v2.2 (2026-07-16)

**결정: 구성 변경 없음 — v2.1 구성(32B+prompt4c+NP=2+상시로드) 유지. 단, 실측의 기준이 바뀐다.**

## v2.2 — 속도 2차 최적화 시도: 전 지렛대 기각, 대신 측정 기반을 교정했다

### v2.1의 35.5s/장은 캐시 오염이었다 (이번 판의 가장 중요한 교정)

`keep_alive:-1`로 살아있는 슬롯이 **직전 런과 동일한 (이미지, 프롬프트)의 KV를 prefix 재사용**한다
— 같은 픽스처를 반복 측정하는 벤치마크에서 prefill이 0.2s로 붕괴(20,000 tok/s처럼 보임).
v2.1의 284s(35.5s/장)가 이것이다. **캐시 퇴거(서로 다른 장문 2발을 NP개 동시 발사) 후 신선 실측:
497s/8 = 62s/장.** 프로덕션의 새 스크린샷은 항상 신선 = 62s/장이 진짜 기준선.
(부산물: 같은 이미지 재추출은 prefill이 거의 무료 — 재시도·재검증엔 유리한 성질.)

### 물리 재분해 (신선 기준, 장당)

| 구간 | 실측 | 성질 |
|---|---|---|
| ViT 인코딩 | ~9.6s | prompt_eval에 섞여 있음(v2.1의 "145tok/s"는 이것 포함한 혼합값) |
| LLM prefill | 4.3~5.3k tok @ **216 tok/s** = 20~24s | compute-bound, 요청 간 직렬 |
| decode | ~300 tok @ 단독 6.9 t/s, NP=2 합산 11.2 t/s | 대역폭 포화 |
| wall(NP=2) | ≈ (prefill합+decode합)/2 ≈ 62s/장 | prefill·decode **공동 병목** |

이미지 토큰 회계: HF 레퍼런스(patch 14·merge 2 → **px/784**)와 ollama가 **일치**(3276 vs 3298).
"ollama가 토큰을 4배 과다 계상한다"는 가설은 스펙 오독이었다(패치 28px 아님). 예외: 4928px
톨스크린은 ollama가 ~4k 토큰으로 캡(레퍼런스 6864) — 그런데도 parity 통과.
→ **vLLM 전환에 '공짜 토큰 절감'은 없다.** 이득은 커널 효율(추정 2~3배)뿐.

### 시도한 지렛대 — 전부 기각 (정확도 게이트에 걸림)

| 지렛대 | wall | 정확도 | 판정 |
|---|---|---|---|
| E2 지시문→system 이동(prefix KV 재사용, -1267tok/장) | 470s(-5%) | **FAIL** — 160229 '미국 달러' 환각 1 + 원화 잔고행 유실(30/31) | ❌ |
| E3 보수 크롭(상태바 90px+소프트내비 110px+가장자리 잉크트림, -9.8% px) | 481s(-3%) | **FAIL** — mPOP 병합셀 평가↔매수 스왑 7건(value 24/31) | ❌ |
| E4 7B 기본 + 게이트 경고 화면만 32B 재추출(파동식) | (207s+에스컬) | **FAIL** — USD 중복행 유실 시 합계 게이트 침묵(FN>0 실증, 아래) | ❌ |
| E5 qwen2.5vl:3b 단독 | 130s(16.3s/장) | **FAIL** — 재현율 0/31, 환각 2(출력 50~175tok로 붕괴) | ❌ |

**E4의 구조적 맹점(중요)**: 게이트는 합계 대조다. 외화예수금 화면에서 7B가 USD 표기행을 떨구고
원화 잔고행만 내면 요약-상세 합계는 여전히 맞는다 → 경고 없이 행 유실. 캐시가 균일하던 어제의
3런 "결정론적 회복(3/8장 교체 PASS)"은 착시였고, 신선 런에선 실패 화면 자체가 달라졌다(비결정).
**행 존재를 보증하지 못하는 게이트 위에 작은 모델을 얹을 수 없다.**

### 이번 판의 교훈

1. **31/31은 안정 평형이 아니라 칼날이다.** 크롭(기하), 프롬프트 위치(system/user), 캐시·배칭
   상태(수치 미세차) — 어느 미세 변화에도 특정 화면의 셀 귀속이 무너졌다. temp=0이어도
   continuous batching의 슬롯 상태에 따라 raw가 달라진다(신선 런 name 1건·price_src 8건 흔들림,
   PASS는 유지). 속도 실험은 반드시 **신선 캐시 + 8장 전체 parity**로만 판정할 것.
2. **속도 벤치마크는 캐시 퇴거가 선행 조건.** 퇴거는 서로 다른 장문을 NP개 **동시** 발사
   (순차 발사는 같은 슬롯만 두 번 덮는다).
3. 측정 하네스: `run_extract.py`가 이제 `prompt_eval_count/duration·eval_count/duration`을 저장,
   `SHOTS`(픽스처 경로)·`SYSTEM_MODE`(기각된 E2 재현용) env 지원.

### 6s/장(10장 1분) 도달 가능성 판정 (신선 물리 기준)

| 경로 | 추정 | 판정 |
|---|---|---|
| 32B+ollama 현 구성 | 62s/장 | 기준선 |
| 32B+vLLM(커널 2~3배 가정) | ~25~30s/장 | 미달. AWQ 재양자화로 정확도 재검증도 필요 |
| 7B/3B 계열(단독·하이브리드) | — | **정확도 불가**(파리티 실패 + 게이트 맹점) |
| OCR-primary(PP-StructureV3 등)+LLM 매퍼 | ~2~4s/장 | **유일한 후보** — 단 한글 조밀표 자릿수 정확도 미검증(스파이크 필요) |

**로컬 VLM 경로로는 6s/장은 물론 15s/장도 안전하게 도달 불가.** 남은 선택지:
(a) OCR-primary 스파이크(가장 어려운 화면 2~3장으로 자릿수 정확도부터), (b) 목표 완화(62s/장 수용,
UX를 비동기로), (c) 클라우드 API(민감정보 — 사용자가 배제).

### 운영 확인 사항 (즉시 조치 가능)

- **프로덕션 인스턴스(11434, systemd)에 드롭인이 없다** — `OLLAMA_NUM_PARALLEL=2`·`OLLAMA_KEEP_ALIVE=-1`
  미설정 상태로 서빙 중일 가능성. v2.1의 배칭 이득이 프로덕션에 실제 적용되고 있는지 불명.
  → `sudo systemctl edit ollama` → `[Service] Environment="OLLAMA_NUM_PARALLEL=2" "OLLAMA_KEEP_ALIVE=-1"`
  (실험은 올바른 env를 가진 포트 11435의 사용자 인스턴스에서 수행했다. 이중 기동 정리 필요.)

---

# (v2.1) 속도 최적화 기록 — ⚠ 아래 s/장 수치는 캐시 오염(v2.2 참조), 상대 비교만 유효

# 로컬 추출 모델 선정 — 결정 v2.1 (2026-07-15)

**결정: `qwen2.5-VL-32B` + `prompt4c`(압축출력) + `OLLAMA_NUM_PARALLEL=2` + 불변식 게이트**
(Ollama / **Jetson AGX Orin 64GB**)
파이프라인: 비전추출(NP=2 동시) → 파서(견고화) → 게이트(합계 대조·불변식) → 증권사 정규화 → 수량 사다리(T1~T4) → 출처 표기.

## v2.1 — 속도 최적화 (정확도 무손실 실측: 92s/장 → 35.5s/장, 2.6배)

병목 분해가 먼저다: 화면당 92s = prefill 31~37s(이미지 4.4~5.1k tok, **compute-bound** ~145tok/s)
+ 생성 64~171s(**대역폭 포화** 6.1tok/s = 이론상한 6.5의 94% — 21GB 가중치를 토큰마다 스트리밍).
콜드로딩 89.6s는 keep_alive 만료 시마다 재발.

| 지렛대 | 실측 | 채택 |
|---|---|---|
| **출력 압축**(`prompt4c` — 지시문은 prompt4 그대로, 출력만 키 없는 positional 배열) | 생성 토큰 -43~53%(1047→603tok) | ✅ 파리티 31/31 유지 확인 |
| **디코드 배칭**(`OLLAMA_NUM_PARALLEL=2` + 화면당 요청 2개 동시 발사) | 2요청 2.37배(223→94s). 한 스텝에 가중치 1회 스트리밍으로 두 시퀀스 생성 + 한쪽 prefill이 다른쪽 디코드와 겹침 | ✅ raw 8/8 재현 동일 |
| np=4 | 2.32배 — np=2에서 이미 포화(prefill이 compute 직렬) | ❌ np=2로 충분 |
| **상시 로드**(`keep_alive:-1` + 기동 warmup) | 첫 장 -90s | ✅ |
| 2장/한 호출 | 이득 ~7s/장(프롬프트 prefill 공유)뿐 + **행→화면 귀속**이 모델 태그 의존으로 전락(finalize의 화면 경계 로직 침식) | ❌ |
| 이미지 리샘플(픽셀 축소) | **기각 실측**: ×0.66 전체 8장 → 재현율 27/31·환각 1·ISA 화면 붕괴, ×0.5 → 자릿수 10배 오류(905,940→9,059,400). 게다가 축소 화면에서 없는 열(cost)을 지어내 출력이 늘어 wall 342s로 **원본(284s)보다 느림**. 부분 사다리(2장)는 통과처럼 보였다 — 표본이 전체를 대변 못함 | ❌ |
| JPEG 품질 압축 | 무효 — 토큰 수는 파일 크기가 아니라 픽셀 치수로 결정 | ❌ |
| MoE 30b-a3b | 2.3배 빠르나 재현율 29/31(현금 2행 유실 — 게이트로 회복 불가) | ❌ |

주의(실측 함정): 두 지렛대는 **완전히 합산되지 않는다** — 압축이 디코드를 줄이면 배칭 불가능한
prefill이 새 병목이 된다(compute 직렬). 그래서 2.37×1.5가 아니라 합계 2.6배.
슬롯 수를 바꾸면 `-c = num_ctx×NP` 재적재 1회가 발생한다(run1 507s에 섞였던 그 비용) — 상시 로드로 1회뿐.

**8장 실측 284s(35.5s/장) · 재현성 raw 8/8 동일 · 파리티 PASS(기준선과 완전 동급) · 게이트 대조군 전부 통과.**
10장 ≈ 6분, 15장 ≈ 9분 (기존 24.5분 → 2.7배). 운영: `NP`(서버)=`OLLAMA_NUM_PARALLEL`(ollama) 일치 필수.
systemd 운영 시: `sudo systemctl edit ollama` → `[Service] Environment="OLLAMA_NUM_PARALLEL=2" "OLLAMA_KEEP_ALIVE=-1"`.

---

# (v2) 모델 선정 기록

**결정: `qwen2.5-VL-32B` + `prompt4` + 불변식 게이트** (Ollama / **Jetson AGX Orin 64GB**)
파이프라인: 비전추출 → 파서(견고화) → 게이트(합계 대조·불변식) → 증권사 정규화 → 수량 사다리(T1~T4) → 출처 표기.

목적함수도 바꿨다: 화면별 채점(`score.py`)이 아니라 **엔드투엔드 파리티**(`parity.py`) —
스크린샷 8장 → 최종 holdings를 정답표(31종목)와 대조. v1이 화면별로 100%를 받고도 파이프라인이
깨졌던 이유가 이것이다(국소 점수는 종합 결과를 보장하지 않는다).

## v1의 전제가 세 개 무너졌다

| v1의 기술 | 실측 |
|---|---|
| "Orin 32GB · 32B는 메모리 3배라 불필요" | **64GB**(가용 48GB). 32B q4 ≈ 20GB — 넉넉히 적재. 근거 소멸 |
| "32B는 평가기 OOM으로 미실행" | 그 평가는 *16GB 기계*에서 했다. Orin에서 실행하니 **32B가 유일한 통과 모델** |
| "7B가 재현율·정확도 100%" | 화면별 채점 기준이었다. 엔드투엔드로는 7B가 값 1건을 계속 틀린다 |

v1이 "남은 검증"으로 미뤄둔 세 항목을 이번에 측정했다:
①속도 33s/장(7B)·92s/장(32B) ②환각저항 → 게이트 대조군으로 검증 ③**재현성 → 3회 반복 분산 0**
(이번 회귀의 원인은 비결정성이 아니라 JSON 파싱 버그였다. 아래.)

## 검토한 모델 (전부 동일 조건: prompt4 · num_ctx 8192 · temp 0 · 같은 파이프라인)

| 모델 | 재현율 | 환각 | 필드 정확도 | 속도 | 판정 |
|---|---|---|---|---|---|
| **qwen2.5vl:32b** ★ | **31/31** | **0** | **전 항목 ✅**(값·수량·주가·원가·증권사·계좌유형·출처) | 92s/장 | **채택** |
| qwen2.5vl:7b | 31/31 | 0 | 값 30/31 — 현금 `4,716`을 `47,160`으로(셀 병합 오독). 요약화면 총액을 손익으로 읽어 **교차검증 게이트가 무력화**(경고 9건) | 33s/장 | 탈락(빠름 — 폴백) |
| qwen3-vl:30b-a3b-instruct | 29/31 | 0 | 값·수량 ✅이나 `AIPO`→`AIP0`, 화면 제목을 증권사로 오인, 계좌유형 축약(IRP 정보 손실) | 40s/장 | 탈락 |
| qwen3-vl:8b-instruct | 29/31 | 2 | 평가금액 자리에 매수금액, **매수금액을 둥근 수로 지어냄**(NVDA 48,000,000) | 30s/장 | 탈락 |
| qwen3-vl:8b (thinking) | — | — | 사고 토큰만 4,553개 생성 후 컨텍스트 초과 → **응답 0자**. `think:false`도 무효 | 228s/장 | 실행 불가 |
| glm-ocr / deepseek-ocr (2단계) | — | — | 32B가 통과해 불필요 | — | 미실행 |

**교훈은 v1과 같되 근거가 더 강해졌다: 크기 ≠ 정확도, 그러나 세대 ≠ 정확도이기도 하다.**
최신 세대(qwen3-vl)가 이 작업(한국 증권사 앱의 조밀 표·한글 라벨)에서 오히려 더 나빴다.
같은 계열의 체급 상승(2.5-7B → 2.5-32B)만이 실제 이득을 냈다.

## 회귀 2건의 진짜 원인 (다음 사람이 같은 함정을 피하도록)

1. **해외주식 10종목(128M) 통째 유실** — 모델·컨텍스트 문제가 아니었다.
   모델이 화면 표기 그대로 `"cost": +263504`를 출력했고 **선행 `+`는 JSON 문법 위반**이라
   `json.loads`가 배열 전체를 거부 → 0행 → `classify([])`가 기본값 `detail` 반환 → 조용한 빈 화면.
   → 파서 견고화(부호·쉼표·트레일링콤마 교정 + **행 단위 salvage**) + 빈 화면은 `empty`로 경고.
2. **삼성 ISA → 한국투자증권 오정규화** — 오염원은 **프롬프트**였다.
   스키마의 자리표시 문자열 `"broker":"증권사명"`을 모델이 그대로 뱉자, `resolve_broker`가 그것을
   브랜드로 오인해 네이버에 "증권사명 어느 증권사"를 검색하고 **검색결과 최빈 증권사를 정답으로 채택·캐시**했다.
   → 스키마에서 자리표시 제거(값 없으면 null) + 자리표시·일반명사는 검색 금지 + **근거 없으면 답하지 않음**
   (LLM이 UNKNOWN이면 최빈 폴백도 하지 않는다) + 캐시 자가치유.

## 파이프라인이 모델 오류를 흡수한다 (모델 무관 안전망)

7B 기준 **환각 7건 → 0건, 수량 21/30 → 31/31**을 *모델을 바꾸지 않고* 회복시킨 불변식들:

- **회계 항등식** `평가금액 = 매수금액 + 평가손익` — 깨지면 지어낸 매수금액으로 보고 재계산.
  (매수금액 열이 없는 화면에서 모델은 손익을 원가 자리에 넣거나 값을 발명한다.)
- **화면 수량 검증** — 캡처일 종가로 설명되지 않는 수량은 기각하고 사다리로 재도출.
  (수량 열이 없는 화면에서 모델이 평가손익을 수량 칸에 넣는다: `qty=998400`.)
- **합계행은 이름이 아니라 산술로** 판정(어떤 행의 값 = 나머지 합 → 그 행은 총액).
  이름 목록에 기대면 '외화예수금' 같은 정당한 현금 보유행까지 지워진다.
- **한 자산 = 한 행** — 같은 금액 반복(예수금 당일/D+1/D+2)은 병합, 외화 잔액의 달러·원화 두 표기도 병합.
- **잔고는 음수일 수 없다 / 잔고 == 손익이면 손익 복제** → 오염된 총액은 대조 기준에서 제외
  (오염된 자[尺]로 대조하면 게이트가 거짓 경보를 낸다).
- **계좌 잔고 행에는 수량이 없다** → 수량 칸의 숫자는 잘못 담긴 잔고다.
- **증권사 상속은 계좌 단위**로만. '증권사가 하나뿐이면 그걸 쓴다'는 폴백은 **금지**(교차오염).
  라벨 없는 화면은 *같은 요약화면에 대조되는 화면들과 같은 앱*이라는 관계로 소속을 추론한다.
- **심볼 검증 게이트** — 이름 검색이 엉뚱한 종목을 집으면(‘메타 플랫폼스’→국내 메타랩스) 캡처일 종가로
  화면 단가가 설명되지 않는다 → 심볼 채택 안 함(조용한 오매칭 차단).

## 수량 추정 사다리 (추측은 반드시 추측이라 표시)

| 단계 | 근거 | qty_src | 신뢰 |
|---|---|---|---|
| T1 | 화면에 적힌 수량 | `screen` | exact |
| T2 | 수량·평가금액 → 주가(USD 자산이면 ÷FX) | — (price_src `computed:value/qty[/FX]`) | exact |
| T3 | **계좌간 동일종목**의 주가로 역산 | `derived:cross-account(단가)` | high |
| T4 | **캡처일(EXIF) 종가**로 역산 | `derived:capture-close(날짜)` | estimated |
| 실패 | 위 전부 불가 | `unobtainable` | **null — 지어내지 않는다** |

T3가 T4보다 위인 이유: 같은 캡처 시점·같은 종목의 주가라 시장 타이밍·환율 오차가 끼지 않는다.
정수 검증은 **게이트**다 — 역산값이 정수로 떨어지지 않으면 반올림해 밀어붙이지 않고 abstain한다.
T4의 노이즈 게이트(`잔차 + 수량×δ < 0.33`, δ: KRW 0.0006 / USD 0.015)는 오채택 0으로 유지.

## 검증 (근거)

- **파리티** `python3 eval/harness/parity.py eval/results/qwen2.5vl_32b_p4 --no-llm`
  → 31/31 · 환각 0 · 전 필드 ✅ · **게이트 경고 0(침묵)** · PASS
- **게이트 대조군**(`--controls`): 화면 누락·빈 화면·환각행 주입 → **3종 모두 경고** ✅ /
  화면 순서 셔플 → **동일 결과** ✅ (투입 순서 의존성 0)
- **재현성**: 동일 설정 3회 반복 → 분산 0(7B 기준 측정. 이번 회귀는 비결정성이 아니었다)
- **실연결 E2E**(`eval/harness/e2e.py`): 헤드리스 Firefox → 앱 → `/extract/batch` → 표·출처칩까지 통과

## 정답표 감사 (자를 먼저 검사했다)

31행 전부를 스크린샷과 1:1 재대조하고 산술 정합성을 재계산했다. **v1 정답표의 '천장'은 가짜였다**:
"당일 시세 미확인"으로 null 처리한 수량 7건이 캡처일 종가로 **6건은 잔차 0.000, 정확히 정수**로 떨어졌다.
그대로 뒀다면 파이프라인이 정답보다 정확한 값을 내고도 오답 처리될 뻔했다 → 근거와 함께 정답표를 갱신.
(요약 4:01 ↔ 해외주식 상세 4:02 사이 15,219원 차이는 실시간 시세 변동 — 오류 아님.)

---
하네스: `eval/harness/`(`run_extract.py` 추출 · `parity.py` 채점·대조군 · `e2e.py` 실연결)
민감정보(스크린샷·정답표·결과·시세·심볼캐시)는 gitignore. **커밋 금지.**
