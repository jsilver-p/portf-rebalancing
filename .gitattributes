# index.html:193 은 앱 문서를 통째로 인라인한 117KB **한 줄**이다. 두 브랜치가 프론트를 조금이라도
# 건드리면 그 한 줄이 양쪽에서 바뀌어 줄 단위 머지는 반드시 충돌한다 — 내용이 겹쳐서가 아니라
# 표현이 한 줄이라서. 전용 드라이버가 그 줄을 디코드해 '내용'으로 3-way 머지한 뒤 다시 심는다.
# 드라이버 등록은 로컬 1회: bash .githooks/setup.sh   (README '프론트엔드 편집' 참조)
index.html merge=pf-app
